cmake_minimum_required(VERSION 3.28.0)
project(2d_cod_zombie VERSION 0.1.0 LANGUAGES C CXX)

include(CTest)
enable_testing()

add_executable(2d_cod_zombie main.cpp map.cpp input.cpp classes.cpp mouse.cpp)

target_link_libraries(2d_cod_zombie SDL2main SDL2 SDL2_image)
set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
